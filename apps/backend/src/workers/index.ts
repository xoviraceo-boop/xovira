/**
 * Worker entry point
 * Run this file to start the matching worker
 * 
 * Usage: tsx src/workers/index.ts
 * or: node dist/workers/index.js
 */

import './matchingWorker';

console.log('[Worker] Starting matching worker and scheduler...');

console.log('[Worker] Worker and scheduler started successfully');
console.log('[Worker] Press Ctrl+C to stop');

